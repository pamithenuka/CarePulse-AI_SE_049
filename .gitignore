# Local development secrets — contains real DB passwords, never commit.
**/appsettings.Development.json

# .NET build output — regenerated by `dotnet build`/`dotnet run`, never commit.
# Previously tracked by mistake; this caused merge conflicts for teammates
# building on their own machines.
[Bb]in/
[Oo]bj/
